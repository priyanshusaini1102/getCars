<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Login</title>
    <link rel="stylesheet" href="css/style.css">
  </head>

  <body>
    <%- include('../partials/navbar') %>
    <div class="container">
      <div class="row login-form">
        <div class="col-md-6 col-sm-12">
          <h2>Login</h2>
          <% if(msg.message==="invalid password" ){ %>
            <p class="p-2 bg-danger text-light">Please Enter a valid password!</p>
            <% } else if(msg.message=='invalid' ) { %>
              <p class="p-2 bg-danger text-light">Please Enter valid Information</p>
            <% } else if(msg.message == 'loggedout' ) { %>
              <p class="p-2 bg-danger text-light">Successfully logged out!</p>
            <% } %>
          <form action="/login" method="POST">
            <div class="form-group">
              <label class="my-1">Email address</label>
              <input
                type="email"
                class="form-control"
                aria-describedby="emailHelp"
                placeholder="Enter email"
                required="true"
                name="email"
              />
            </div>
            <div class="form-group">
              <label class="my-1"> Enter Password</label>
              <input
                type="password"
                class="form-control"
                name="password"
                placeholder="Password"
                required="true"
                name="password"
              />
            </div>
            <button type="submit" class="btn btn-primary my-1">Login</button>
          </form>
          <a href="signup" class="mt-2">Doesn't have account? SignUP</a>
        </div>
      </div>
    </div>
  </body>
</html>
